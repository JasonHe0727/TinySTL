#ifndef STRING_HPP
#define STRING_HPP

#include <cstdint>

typedef uint16_t Char;

//class String
//{
//private:
//    Char *characters;
//    int length;
//public:
//    String(); // default constructor
//    String(const char *s); // c-string
//    String(const String &s); // copy constructor
//    String(String &&s) noexcept; // move constructor
//    ~String();
//
//    String &operator=(String other);
//
//private:
//    Char *CopyString(const Char *input, int length);
//
//    static int CountUtf8ToUtf16Length(const char* s);
//};

#endif //STRING_HPP
